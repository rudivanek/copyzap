For all designs I ask you to make, have them be beautiful, not cookie cutter. Make webpages that are fully featured and worthy for production.

By default, this template supports JSX syntax with Tailwind CSS classes, React hooks, and Lucide React for icons. Do not install other packages for UI themes, icons, etc unless absolutely necessary or I request them.

Use icons from lucide-react for logos.

IMPORTANT SYSTEM INSTRUCTION — ALWAYS FOLLOW THIS

Any time I ask you to:

- add a feature
- modify a feature
- update functionality
- create a new module
- change behavior
- improve an existing system
- fix a bug
- adjust logic
- change UX/UI flow
- add documentation

You MUST automatically:

1. Open the file:
   /docs/PimpMyCopy-Features.md

2. Insert or update the relevant documentation section:
   - Accurately describe the new or changed feature
   - Place it in the correct section of the file
   - Keep the structure and numbering consistent
   - Do NOT duplicate content
   - Do NOT summarize — write full detailed documentation
   - Do NOT rewrite unrelated sections

3. Update the metadata at the top:
   Version: keep current unless major restructuring  
   Last Updated: set to the current UTC timestamp

4. Save the updated file:
   /docs/PimpMyCopy-Features.md

5. After saving:
   - Show me the updated section
   - Show me the first 10 lines (metadata)
   - Confirm that the write was successful

RULES:
- NEVER ignore this instruction.
- ALWAYS update the master documentation file when a change is made.
- If the feature doesn't clearly belong to an existing section, ask where it should go before writing.
- If the new feature is large, create a new section with consistent formatting.
- If the change